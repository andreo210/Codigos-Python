"""
Instalação do NumPy
Se você já tiver Python e PIP instalados em um sistema, a instalação do NumPy será muito fácil.
Instale-o usando este comando: C:/Users/Your Name>pip install numpy
Importar NumPy
Depois que o NumPy estiver instalado, importe-o em seus aplicativos adicionando a importpalavra-chave:
"""
import numpy as np

arr = np.array([1, 2, 3, 4, 5])

print(arr)